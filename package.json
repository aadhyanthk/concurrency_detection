{
  "name": "concur-detec",
  "version": "1.0.0",
  "scripts": {
    "frontend-install": "cd src && npm install",
    "frontend-dev": "cd src && npm run dev",
    "backend-dev": "uvicorn api.main:app --reload",
    "build": "cd src && npm install && npm run build"
  }
}